services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: licio
      POSTGRES_USER: licio
      POSTGRES_PASSWORD: licio
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
